---
import Image from "astro/components/Image.astro"

const { title, imagePath, width, height } = Astro.props
---

<Image src={imagePath} alt={title} loading="lazy" width={width} height={height} class="h-64 w-full object-cover rounded-xl opacity-80"/>